// import { computed } from "vue";
// import { store } from "../storage";
// import type { Mod } from "../../structs/mission_structs";

// var modData = computed(() => store.getters.getMod as Mod);
// export const countryNameToColorMap = computed(() => new Map(modData.value.countries.map(country => [country.tag, country.color])));

// export const countryTagToCountry = computed(() => new Map(modData.value.country_tags.map(country => [country.tag, country])));