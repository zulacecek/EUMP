// import { computed } from "vue";
// import { store } from "../storage";
// import type { Mod } from "../../structs/mission_structs";

// var modData = computed(() => store.getters.getMod as Mod);
// export const religionNameToColorMap = computed(() => new Map(modData.value.religious_groups.flatMap(group => group.religions.map(religion => [religion.name, religion.color]))));