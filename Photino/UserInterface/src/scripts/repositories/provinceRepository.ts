import { computed } from "vue";
import type { Mod } from "../../structs/missionStructs";

// var modData = computed(() => store.getters.getMod as Mod);
// export const colorToIdMap = computed(() => {
//    return new Map(modData.value.provinces.map(x => [x.color, x.id ]));
// });

// export const provinceIdToProvince = computed(() => {
//    return new Map(modData.value.provinces.map(x => [x.id, x]));
// });