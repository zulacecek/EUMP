// import { computed } from "vue";
// import { store } from "../storage";
// import type { Mod } from "../../structs/mission_structs";

// var modData = computed(() => store.getters.getMod as Mod);
// export const provinceIdToTradeNode = computed(() => new Map(modData.value.trade_nodes.flatMap(tradeNode => tradeNode.members.map(provinceId => [provinceId, tradeNode]))));